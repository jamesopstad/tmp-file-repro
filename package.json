{
  "name": "tmp-file-repro",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dry-run": "wrangler deploy --config src/wrangler.jsonc --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.12.1"
  }
}
